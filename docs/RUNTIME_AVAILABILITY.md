# Runtime availability matrix

Generated by `scripts/check-runtime-availability.mjs` from
`docs/runtime-availability.json`, which
`apps/web/src/runtime-availability.test.tsx` writes while driving each
operation through the real production entry point
(`apps/web/src/main.tsx → components/ProductShell`).

**Why this exists rather than a code search.** A static check for
`disabled={true}` proves nothing: an expression can be present, dynamic, and
still evaluate false forever. Each row below was produced by rendering the
shipped shell, observing the control refused, satisfying exactly the
prerequisites the application itself names, and observing it become
available. The suite fails if any of those steps does not happen.

**What this is not.** Nothing here is a hardware claim. The device is a
fake driver that answers CRSF correctly; the rows prove the application's
own gating and wiring, not that a transmitter did anything.

13 rows.

| Operation | Surface | Transport | Visible control | Readiness inputs | Refused before | Enabled after | Handler | Driver | Write authority | Recovery checkpoint | Verification | On failure |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `airport` | advanced | browser | AirPort transparent serial (checkbox) | - a Target chosen | yes | **yes** | `useDeviceController.updateOption('airportEnabled')` | firmware-package: writes the is-airport option key, which no rx-as-tx value ever sets | inherited from firmwareWrite; this only shapes the package | as for any firmware write | the package's option block is asserted, not the device role | as for any firmware write |
| `binding` | advanced | browser | Run the real binding | - not busy<br>- confirmed CRSF identity<br>- previous port confirmed closed<br>- recovery journal read<br>- no pending checkpoint<br>- the operator's binding acknowledgement | yes | **yes** | `useDeviceController.runBinding` | session.startBinding → CRSF command 0x32 to the Bind parameter | single-use capability for BINDING | none taken; binding changes no stored image, so there is nothing to restore | TX_BIND_COMMAND_ACKNOWLEDGED from the device; a matching UID is proven by a live link, not by this write | the acknowledgement is cleared and the reason is named |
| `bindingPrerequisites` | advanced | browser | The other end is ready to bind… (checkbox) | - not busy<br>- confirmed CRSF identity<br>- previous port confirmed closed<br>- recovery journal read<br>- no pending checkpoint | yes | **yes** | `useDeviceController.setBindingAcknowledged` | none; this is the operator's own confirmation | none; it exists so binding can be gated on a confirmation without being gated on itself | not applicable | not applicable | not applicable |
| `connect` | advanced | android | Identify the device over CRSF | - not busy<br>- previous port confirmed closed<br>- a native bridge injected by the packaged host | yes | **yes** | `the same useDeviceController.connectHardware` | nativeBridgeNavigator(bridge) is handed to the same session layer, so CRSF, flashing and recovery are the browser's code paths | the same capability model, plus the host's own session ownership | the same checkpoint | the same CRSF Device Info verification | a host that could not install its bridge reports the exact reason rather than an empty device list |
| `connect` | advanced | browser | Identify the device over CRSF | - not busy<br>- previous port confirmed closed | no — it has no prerequisites | **yes** | `useDeviceController.connectHardware` | userSession.connectHardwareDriver → navigator.serial @ 420000 | not required; reading identity is not a device change | read on mount; a pending one blocks writes, not this | CRSF Device Info 0x29; role from the origin address | named transport reason; the control returns to enabled |
| `diagnostics` | advanced | browser | Show the report | - none — it has no prerequisites by design | no — it has no prerequisites | **yes** | `DiagnosticsPanel.show → useDeviceController.captureDiagnostics` | reads the live session and the browser's own capability answers | none; it writes nothing to a device | reported, not changed | not applicable; it reports observations, it does not act | a browser that refuses to answer yields null rather than a failed export |
| `firmwareWrite` | advanced | browser | Start the real flash | - not busy<br>- a Target chosen<br>- previous port confirmed closed<br>- recovery journal read<br>- no pending checkpoint<br>- a prepared package<br>- the recovery archive downloaded<br>- the power acknowledgement<br>- the antenna acknowledgement (transmitters)<br>- the manual Target confirmation, where the Target was not auto-matched<br>- a confirmed CRSF identity, for the UART method | yes | **yes** | `useDeviceController.flashPreparedFirmware` | esp-flasher.flashEspFirmware (esptool-js) / xmodem / stm32-dfu, chosen by the Target's platform and the upload method | single-use capability for FIRMWARE_WRITE, TTL 180s | written before the first byte; kept until the device is verified back | segment read-back and reconnect; success requires the expected role, so an rx-as-tx write that comes back a receiver fails | the checkpoint stands, recovery becomes available, and the state is WRITE_COMPLETED_RECONNECT_UNVERIFIED rather than SUCCESS |
| `firmwareWrite` | easy | browser | Write the firmware to the device | - not busy<br>- a confirmed CRSF identity<br>- a Target and a regulatory region chosen<br>- a prepared, verified package<br>- the recovery archive downloaded<br>- the operator's confirmation that the recovery file is kept<br>- the power acknowledgement<br>- the antenna acknowledgement (transmitters) | yes | **yes** | `EasySetup → useDeviceController.flashPreparedFirmware` | the same esp-flasher / xmodem / stm32-dfu path the Advanced view uses; Easy Mode is a view, not a second implementation | single-use capability for FIRMWARE_WRITE, as in Advanced | the same checkpoint, written by the same controller | the same read-back and reconnect verification | the same recovery path, surfaced in the operator's language |
| `recovery` | advanced | browser | Choose the recovery package (file input) | - not busy<br>- a Target chosen<br>- previous port confirmed closed<br>- recovery journal read<br>- a pending checkpoint or a downloaded recovery archive<br>- the power acknowledgement<br>- the antenna acknowledgement (transmitters)<br>- the Target typed back, because a checkpoint names a device this session may not have identified | yes | **yes** | `useDeviceController.recoverFromFile` | validateRecoveryPackage, then the same flashing driver the original write used | its own single-use capability for RECOVERY | the checkpoint is what makes this available, and it is cleared only once the device is verified back | the restored image is read back and the device must reconnect as the identity the checkpoint recorded | the checkpoint stands so recovery can be retried; it is never cleared by a failed attempt |
| `recoveryImport` | advanced | browser | Restore the device from the imported package | - not busy<br>- a Target chosen<br>- previous port confirmed closed<br>- a recovery file picked from storage the operator owns<br>- the file authenticated with the operator's passphrase<br>- the recovered device identity confirmed by the operator<br>- the power acknowledgement<br>- the antenna acknowledgement (transmitters)<br>- the Target typed back | yes | **yes** | `useDeviceController.recoverFromImportedPackage` | the same flashing driver a picked-file recovery uses; the source of the bytes differs and nothing else does | its own single-use capability for RECOVERY | reconstituted from the imported package's own digest, because an uninstall erased the journal — which is the case this path exists for | the same read-back and post-write identity verification as any recovery | the imported package is kept in state, so the restore can be retried without picking the file again |
| `rxAsTx` | advanced | browser | Flash this receiver with transmitter firmware (select) | - a Target chosen<br>- the Target's platform supports the mode, read from the pinned upstream rules | yes | **yes** | `useDeviceController.updateOption('rxAsTxMode')` | firmware-package: the _TX artifact is selected and the receiver's layout rewritten per mode | inherited from firmwareWrite; this only shapes the package | the checkpoint records the receiver the operator started from, so restoring puts the receiver role back | the device must come back reporting the transmitter address; otherwise RX_AS_TX_ROLE_NOT_APPLIED | the checkpoint is kept and the original receiver image stays recoverable |
| `settingsRestore` | advanced | browser | Restore the snapshot | - not busy<br>- confirmed CRSF identity<br>- previous port confirmed closed<br>- recovery journal read<br>- no pending checkpoint<br>- a settings snapshot — taken on connect, before anything is written | yes | **yes** | `useDeviceController.restoreSettingsBackup` | session.writeParameter, replaying the snapshot's values | its own single-use capability for SETTINGS_RESTORE; a capability granted for the write cannot be reused here | the snapshot itself is the checkpoint for settings; it is not cleared by a failed restore | each replayed parameter is read back and compared | the snapshot is kept and the restore can be retried |
| `settingsWrite` | advanced | browser | Save, with read-back | - not busy<br>- confirmed CRSF identity<br>- previous port confirmed closed<br>- recovery journal read<br>- no pending checkpoint<br>- a writable setting selected | yes | **yes** | `useDeviceController.writeSelectedSetting` | session.writeParameter → CRSF parameter write, then read-back | single-use capability for SETTINGS_WRITE, bound to session, device fingerprint and operation | a settings snapshot is taken before the write; Restore returns to it | the parameter is read back and compared; VERIFIED or not | the snapshot stands and Restore the snapshot stays available |

